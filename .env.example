# Provider: "groq" (free) or "openai"
LLM_PROVIDER=groq

# Groq (free) — get key at https://console.groq.com
GROQ_API_KEY=gsk_...

# OpenAI (paid) — only needed if LLM_PROVIDER=openai
OPENAI_API_KEY=sk-...
